# Add these to the backend environment. Never include credentials in UI builds.
NOTIFICATION_DELIVERY_ENABLED=false
SMTP_HOST=
SMTP_PORT=465
SMTP_SECURE=true
MAIL_NAME=
MAIL_PASSWORD=
SMS_PROVIDER=twilio
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=
